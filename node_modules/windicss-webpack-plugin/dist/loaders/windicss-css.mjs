import { t as transformCSS } from '../shared/windicss-webpack-plugin.e34314d2.mjs';
import 'debug';

function WindicssCss(source) {
  if (!this._compiler)
    return source;
  this.cacheable(true);
  const service = this._compiler.$windi;
  if (!service)
    return source;
  return transformCSS(service, source, this.resource);
}

export { WindicssCss as default };
